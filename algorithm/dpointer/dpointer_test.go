package dpointer

import (
	"testing"
)

func TestDPointer(t *testing.T) {
	// two_sum: [1,2], [1,3], [1,2]
	// t.Log(twoSum([]int{2, 7, 11, 15}, 9))
	// t.Log(twoSum([]int{2, 3, 4}, 6))
	// t.Log(twoSum([]int{-1, 0}, -1))

	// merge_sorted_array: [1,2,2,3,5,6], [1], [1], [1,2]
	// merge([]int{1, 2, 3, 0, 0, 0}, 3, []int{2, 5, 6}, 3)
	// merge([]int{1}, 1, []int{}, 0)
	// merge([]int{0}, 0, []int{1}, 1)
	// merge([]int{2, 0}, 1, []int{1}, 1)

	// linked_list_cycle_ii: 2
	// n0 := &ListNode{3, nil}
	// n1 := &ListNode{2, nil}
	// n2 := &ListNode{0, nil}
	// n3 := &ListNode{-4, nil}
	// n0.Next = n1
	// n1.Next = n2
	// n2.Next = n3
	// n3.Next = n1
	// t.Log(detectCycle(n0).Val)

	// minimal_window_substring: "BANC", "a", ""
	// t.Log(minWindow("ADOBECODEBANC", "ABC"))
	// t.Log(minWindow("a", "a"))
	// t.Log(minWindow("a", "aa"))

	// valid_palindrome_ii: true, true, false, false, true
	// t.Log(validPalindrome("aba"))
	// t.Log(validPalindrome("abca"))
	// t.Log(validPalindrome("abc"))
	// t.Log(validPalindrome("eeccccbebaeeabebccceea"))
	// t.Log(validPalindrome("ebcbbececabbacecbbcbe"))
}
